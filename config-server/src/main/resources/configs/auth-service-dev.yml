# ============================================================
# AUTH SERVICE — DEV PROFILE overrides
# ============================================================
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/lms_auth
