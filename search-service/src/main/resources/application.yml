spring:
  application:
    name: search-service
  config:
    import: "optional:configserver:http://${CONFIG_SERVER_USERNAME:config-admin}:${CONFIG_SERVER_PASSWORD:config-secret}@${CONFIG_SERVER_HOST:localhost}:8888"
